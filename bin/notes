#!/usr/bin/env bash
set -euo pipefail
ROOT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
if [ -x "$ROOT_DIR/.venv/bin/python3" ]; then
  exec "$ROOT_DIR/.venv/bin/python3" "$ROOT_DIR/scripts/notes_manager.py" "$@"
else
  exec python3 "$ROOT_DIR/scripts/notes_manager.py" "$@"
fi
